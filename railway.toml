[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/railway/Dockerfile.executor"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
